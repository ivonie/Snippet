<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nodomain.ivonne.snippet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".servicios.segundoPlano"
            android:theme="@style/miDialogo" />
        <activity
            android:name=".configuracionESP.snippetNuevo"
            android:theme="@style/miDialogo" />
        <activity
            android:name=".configuracionESP.snippetMac"
            android:theme="@style/miDialogo" />
        <activity
            android:name=".configuracionESP.snippetGuardar"
            android:theme="@style/miDialogo" />

        <service
            android:name=".servicios.segundoPlanoService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".desplegarDispositivos"
            android:label="@string/title_activity_desplegar_dispositivos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".configuracionESP.seleccionarImagen"
            android:theme="@style/miDialogo" />
        <activity
            android:name=".configuracionESP.snippetEditar"
            android:theme="@style/AppTheme" />
        <activity android:name=".timerActivity" />
        <activity android:name=".dimmerActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <service
            android:name=".servicios.monitorWiFi"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>